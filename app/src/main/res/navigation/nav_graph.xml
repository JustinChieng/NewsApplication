<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.justin.ui.fragments.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_newsFeedFragment44"
            app:destination="@id/newsFeedFragment4" />
    </fragment>

    <fragment
        android:id="@+id/newsFeedFragment4"
        android:name="com.justin.ui.fragments.NewsFeedFragment"
        android:label="News App"
        tools:layout="@layout/news_feed_list_items">

        <action
            android:id="@+id/action_newsFeedFragment4_to_detailsFragment"
            app:enterAnim="@anim/slide_from_right"
            app:exitAnim="@anim/slide_to_left"
            app:popEnterAnim="@anim/slide_from_left"
            app:popExitAnim="@anim/slide_to_right"
            app:destination="@id/detailsFragment" />

    </fragment>

    <fragment
        android:id="@+id/detailsFragment"
        android:name="com.justin.ui.fragments.DetailsFragment"
        android:label="Details"
        tools:layout="@layout/fragment_details">


        <argument
            android:name="imageUrl"
            app:argType="string" />


        <argument
            android:name="articleContent"
            app:argType="string" />

        <argument
            android:name="articleTitle"
            app:argType="string" />

        <argument
            android:name="articleDate"
            app:argType="string" />
    </fragment>
</navigation>
